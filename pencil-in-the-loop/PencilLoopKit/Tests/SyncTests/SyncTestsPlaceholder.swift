import XCTest

/// Placeholder so the `SyncTests` target has something to compile. It exists to
/// prove the test target is wired up, not to test anything. Replace it.
final class SyncTestsPlaceholder: XCTestCase {
    func testTargetBuilds() {
        XCTAssertTrue(true)
    }
}
