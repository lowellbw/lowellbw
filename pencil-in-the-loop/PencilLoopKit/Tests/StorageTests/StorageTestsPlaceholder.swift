import XCTest

/// Placeholder so the `StorageTests` target has something to compile. It exists to
/// prove the test target is wired up, not to test anything. Replace it.
final class StorageTestsPlaceholder: XCTestCase {
    func testTargetBuilds() {
        XCTAssertTrue(true)
    }
}
